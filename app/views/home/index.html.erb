<% if logged_in? %>
<%= redux_store 'AppStore' %>
<%= redux_router 'AppRoutes' %>
<% else %>

<div class="home">
<nav class="navbar navbar-default navbar-fixed-top">
 <div class="container">
   <div class="navbar-header">
     <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
       <span class="icon-bar"></span>
       <span class="icon-bar"></span>
       <span class="icon-bar"></span>
     </button>
     <a class="navbar-brand" href="#">Cv-App</a>
   </div>
   <div class="collapse navbar-collapse" id="myNavbar">
     <ul class="nav navbar-nav navbar-right">
       <li><a href="#about">ABOUT</a></li>
       <li><a href="#join_us">JOIN US</a></li>
     </ul>
   </div>
 </div>
</nav>
</div>

<div class="jumbotron text-center .bg-jumbotron" style="background-color: #d9534f; color: #ffffff" >
  <h1>Cv-App</h1>
  <p>Create quickly and easily professional-looking CV</p>
</div>

<div id="about" class="container-fluid text-center bg-white">
  <h2>Create cv in 3 steps:</h2>
  <br>
  <div class="row">
    <div class="col-sm-4">
      <span class="glyphicon glyphicon-pencil logo-small"></span>
      <h4>1. Fill in the data</h4>
    </div>
    <div class="col-sm-4">
      <span class="glyphicon glyphicon-file logo-small"></span>
      <h4>2. Choose your design</h4>
    </div>
    <div class="col-sm-4">
      <span class="glyphicon glyphicon-wrench logo-small"></span>
      <h4>3. Generate pdf</h4>
    </div>
  </div>
  </div>
  <div class="container-fluid text-center bg-grey">
    <h2>It is easy as pie!</h2>
  </div>
  <div class="container-fluid text-center bg-white">

  <h2>We offer:</h2>
  <br>
  <div class="row">
    <div class="col-sm-4">
      <span class="glyphicon glyphicon-floppy-disk logo-small"></span>
      <h4>Space</h4>
      <p>On your account you can have as many cvs as you want!</p>
    </div>
    <div class="col-sm-4">
      <span class="glyphicon glyphicon-heart logo-small"></span>
      <h4>Templates</h4>
      <p>Mind-blowing templates for your cv</p>
    </div>
    <div class="col-sm-4">
      <span class="glyphicon glyphicon-check logo-small"></span>
      <h4>Adjustment</h4>
      <p>You can pick which category your cv should include</p>
    </div>
  </div>
</div>

<div id="join_us" class="container-fluid bg-grey">
 <h2 class="text-center">JOIN US</h2>
 <div class="row">
     <%= render 'home/log_in' %>
     <%= render 'home/sing_in' %>
 </div>
</div>

<script>
$(document).ready(function(){
  // Add smooth scrolling to all links in navbar + footer link
  $(".navbar a, footer a[href='#myPage']").on('click', function(event) {

   // Make sure this.hash has a value before overriding default behavior
  if (this.hash !== "") {

    // Prevent default anchor click behavior
    event.preventDefault();

    // Store hash
    var hash = this.hash;

    // Using jQuery's animate() method to add smooth page scroll
    // The optional number (900) specifies the number of milliseconds it takes to scroll to the specified area
    $('html, body').animate({
      scrollTop: $(hash).offset().top
    }, 900, function(){

      // Add hash (#) to URL when done scrolling (default click behavior)
      window.location.hash = hash;
      });
    } // End if
  });
})
</script>
<% end %>
